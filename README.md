Create a docker image using (docker build -t <name> )
Create a container using this image ( docker run -d -p 8787:80 --name <container name> <image name>
Then access the webpages using the default ip with the given port no *in my case i have given 8787*, eg- (127.0.0.1:8787)
